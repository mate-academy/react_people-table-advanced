export enum FilterType {
  All = 'All',
  Male = 'Male',
  Female = 'Female',
}

export enum SortType {
  og = 'og',
  asc = 'asc',
  desc = 'desc',
}

export enum PropName {
  Name = 'name',
  Sex = 'sex',
  Born = 'born',
  Died = 'died',
}
