export enum SortingCells {
  NAME = 'name',
  SEX = 'sex',
  BORN = 'born',
  DIED = 'died',
}

export enum SearchConst {
  SORT = 'sort',
  ALL = 'all',
  SEX = 'sex',
  CENTURIES = 'centuries',
  QUERY = 'query',
  ORDER = 'order',
}

export enum SearchSexParams {
  FEMALE = 'f',
  ALL = 'all',
  MAN = 'm',
}
