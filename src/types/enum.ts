export enum Options {
  ALL = 'All',
  MALE = 'Male',
  FEMALE = 'Female',
}

export enum PersonSex {
  MALE = 'm',
  FEMALE = 'f',
}

export enum TableHead {
  Name = 'name',
  Sex = 'sex',
  Born = 'born',
  Died = 'died',
}
