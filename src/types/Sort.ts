export enum SortBy {
  'name' = 'name',
  'sex' = 'sex',
  'born' = 'born',
  'died' = 'died',
}

export enum SortOrder {
  'decs' = 'decs',
  'asc' = 'asc',
  'off' = 'off',
}
export enum SortQueries {
  'order' = 'order',
  'sort' = 'sort',
}
