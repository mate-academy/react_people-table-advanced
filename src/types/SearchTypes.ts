export enum SearchOptionsType {
  Sort = 'sort',
  Order = 'order',
  Query = 'query',
  Sex = 'sex',
  Centuries = 'centuries',
}

export enum SortListType {
  Name = 'name',
  Sex = 'sex',
  Born = 'born',
  Died = 'died',
}
