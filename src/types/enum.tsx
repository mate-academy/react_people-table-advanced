export enum FilterType {
  All = 'All',
  Male = 'Male',
  Female = 'Female',
}

export enum SortType {
  original = 'original',
  asc = 'asc',
  desc = 'desc',
}

export enum PropName {
  None = '',
  Name = 'name',
  Sex = 'sex',
  Born = 'born',
  Died = 'died',
}
