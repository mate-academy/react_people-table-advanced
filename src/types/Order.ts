export enum Order {
  asc = 'asc',
  desc = 'desc',
}

export enum SortingOptions {
  SortOptions = 'sort',
  OrderOptions = 'order',
}

export enum FilterBy {
  Sex = 'sex',
  Centuries = 'centuries',
}

export enum QueryParam {
  SEARCH = 'query',
}

export enum SortBy {
  NAME = 'name',
  DATE = 'date',
  RELEVANCE = 'relevance',
}
